# Neon PostgreSQL — connection string do dashboard (https://console.neon.tech)
DATABASE_URL="postgresql://user:password@ep-xxx.sa-east-1.aws.neon.tech/netfor?sslmode=require"

# Secret do webhook de revalidação de cache (gerar com: openssl rand -hex 32)
REVALIDATE_SECRET=""

# URL do endpoint de revalidação chamado pelo scraper (ex.: https://netfor.club/api/revalidate)
REVALIDATE_URL=""

# API-Football (https://dashboard.api-football.com) — free tier 100 req/dia
API_FOOTBALL_KEY=""

# Modo B: reescrita de matérias com LLM (Fase futura; manter false no Modo A)
REWRITE_MODE="false"

# Google AdSense (preencher após aprovação — os placeholders viram anúncios reais)
NEXT_PUBLIC_ADSENSE_CLIENT=""
NEXT_PUBLIC_ADSENSE_SLOT_LEADERBOARD=""
NEXT_PUBLIC_ADSENSE_SLOT_RECTANGLE=""
